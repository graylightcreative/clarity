#pragma once

#include <array>
#include <cstdint>

namespace Licensing {
    // Obfuscated HMAC-SHA256 secret key (32 bytes)
    // This file is GENERATED by CMake during the build process
    // The actual secret is injected from GitHub Actions Secrets: GRAYLIGHT_HMAC_SECRET_KEY
    //
    // DO NOT COMMIT THIS FILE - IT CONTAINS SECRETS
    // It is gitignored and generated only during the build

    constexpr std::array<uint8_t, 32> HMAC_SECRET_OBFUSCATED = {
        @CMAKECONFIG_HMAC_SECRET_OBFUSCATED_BYTES@
    };

    // De-obfuscation XOR key (changes per build)
    // This changes every build to prevent pattern analysis
    constexpr uint8_t XOR_KEY = @CMAKECONFIG_XOR_KEY@;

    // Build timestamp (for version checking)
    constexpr uint64_t BUILD_TIMESTAMP = @CMAKECONFIG_BUILD_TIMESTAMP@;
}
